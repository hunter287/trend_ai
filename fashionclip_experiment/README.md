# FashionCLIP vs Ximilar - Эксперимент по сравнению

Эксперимент для сравнения качества распознавания одежды между моделями FashionCLIP и Ximilar.

## 📋 Описание

Этот эксперимент сравнивает две системы распознавания одежды:
- **Ximilar** - коммерческое API (уже используется в проекте)
- **FashionCLIP** - open-source модель на базе CLIP от Hugging Face

### Что сравниваем:
1. **Классификация категорий** (jacket, dress, shirt, pants, etc.)
2. **Детекция цветов** (black, white, brown, multicolor, etc.)
3. **Определение материалов** (cotton, leather, synthetic, etc.)
4. **Распознавание стилей** (casual, formal, elegant, etc.)

### Датасет:
- 100-150 случайных изображений из вашей MongoDB базы
- Только изображения с успешными Ximilar результатами
- Ручная оценка качества через HTML интерфейс

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
cd fashionclip_experiment
pip install -r requirements.txt
```

**Примечание:** Установка может занять несколько минут, особенно PyTorch.

### Шаг 2: Подготовка датасета

Выбрать случайные изображения из MongoDB:

```bash
python prepare_dataset.py
```

Результат: `data/sample_images.json` с метаданными Ximilar

### Шаг 3: Запуск FashionCLIP

Обработать изображения через FashionCLIP:

```bash
python run_fashionclip.py
```

⚠️ **Важно:**
- Первый запуск загрузит модель (~1GB)
- Обработка ~120 изображений займёт 10-30 минут
- С GPU значительно быстрее

Результат: `data/fashionclip_results.json` с результатами обеих систем

### Шаг 4: Генерация HTML отчёта

Создать интерактивный отчёт для оценки:

```bash
python generate_html_report.py
```

Результат: `comparison_report.html` - откройте в браузере

### Шаг 5: Ручная оценка

1. Откройте `comparison_report.html` в браузере
2. Для каждого изображения оцените:
   - Какая система лучше определила одежду
   - Опциональный комментарий
3. Нажмите "Сохранить оценки"
4. Скачается файл `evaluations.json`

## 📁 Структура файлов

```
fashionclip_experiment/
├── README.md                    # Этот файл
├── requirements.txt             # Python зависимости
├── prepare_dataset.py           # Шаг 1: Выборка из MongoDB
├── run_fashionclip.py          # Шаг 2: Запуск FashionCLIP
├── generate_html_report.py     # Шаг 3: Генерация отчёта
├── data/
│   ├── sample_images.json      # Датасет с Ximilar результатами
│   └── fashionclip_results.json # Результаты обеих систем
├── comparison_report.html       # HTML отчёт для оценки
└── evaluations.json            # Ваши оценки
```

## 🔧 Параметры

### prepare_dataset.py

```python
SAMPLE_SIZE = 120  # Количество изображений (100-150)
```

### run_fashionclip.py

```python
MODEL_NAME = 'patrickjohncyh/fashion-clip'  # Модель FashionCLIP

# Словари атрибутов можно расширить:
ATTRIBUTE_PROMPTS = {
    'categories': [...],  # Категории одежды
    'colors': [...],      # Цвета
    'materials': [...],   # Материалы
    'styles': [...]       # Стили
}
```

## 📊 Формат результатов

### sample_images.json

```json
{
  "metadata": {
    "created_at": "2025-01-16T12:00:00",
    "total_samples": 120
  },
  "samples": [
    {
      "id": "...",
      "image_url": "http://...",
      "ximilar_results": {
        "categories": [{"name": "jacket", "confidence": 0.95}],
        "colors": [{"name": "black", "confidence": 0.89}],
        "materials": [{"name": "leather", "confidence": 0.87}],
        "styles": [{"name": "casual", "confidence": 0.92}]
      },
      "fashionclip_results": null
    }
  ]
}
```

### fashionclip_results.json

Тот же формат, но с заполненным `fashionclip_results`:

```json
{
  "fashionclip_results": {
    "categories": [
      {"name": "jacket", "confidence": 0.78},
      {"name": "coat", "confidence": 0.15},
      {"name": "blazer", "confidence": 0.07}
    ],
    "colors": [...],
    "materials": [...],
    "styles": [...]
  }
}
```

### evaluations.json

```json
{
  "metadata": {
    "created_at": "2025-01-16T14:00:00",
    "total_evaluations": 120
  },
  "evaluations": [
    {
      "sample_id": "...",
      "image_url": "http://...",
      "evaluation": "ximilar",  // или "fashionclip" или "equal"
      "comment": "Ximilar точнее определил цвет",
      "evaluated_at": "2025-01-16T14:05:00"
    }
  ]
}
```

## 🎯 Анализ результатов

После завершения оценки вы можете проанализировать:

```python
import json

with open('evaluations.json', 'r') as f:
    data = json.load(f)

evaluations = data['evaluations']

# Подсчёт побед
ximilar_wins = sum(1 for e in evaluations if e['evaluation'] == 'ximilar')
fashionclip_wins = sum(1 for e in evaluations if e['evaluation'] == 'fashionclip')
equal = sum(1 for e in evaluations if e['evaluation'] == 'equal')

print(f"Ximilar лучше: {ximilar_wins} ({ximilar_wins/len(evaluations)*100:.1f}%)")
print(f"FashionCLIP лучше: {fashionclip_wins} ({fashionclip_wins/len(evaluations)*100:.1f}%)")
print(f"Одинаково: {equal} ({equal/len(evaluations)*100:.1f}%)")
```

## 🐛 Troubleshooting

### Ошибка подключения к MongoDB

```bash
# Проверьте переменные окружения
cat ../.env | grep MONGODB

# Или измените в prepare_dataset.py:
mongodb_uri = 'mongodb://localhost:27017/instagram_gallery'
```

### Нет GPU

FashionCLIP будет работать на CPU (медленнее). Для ускорения:
- Уменьшите `SAMPLE_SIZE` в `prepare_dataset.py`
- Или используйте Google Colab с GPU

### Ошибка загрузки модели

```bash
# Очистите кэш Hugging Face
rm -rf ~/.cache/huggingface/

# Повторите установку
pip install --upgrade transformers torch
```

### Ошибки при загрузке изображений

Некоторые URL могут быть недоступны. Скрипт автоматически пропускает их.

## 💡 Советы

1. **Первый запуск**: Начните с малого датасета (20-30 изображений) для проверки
2. **GPU vs CPU**: На GPU обработка в 10-20 раз быстрее
3. **Batch processing**: Можно разбить на несколько сессий
4. **Backup**: Сохраняйте промежуточные результаты

## 📈 Что дальше?

После анализа результатов вы можете:

1. **Если FashionCLIP лучше**:
   - Интегрировать в production
   - Экономия на API calls Ximilar

2. **Если Ximilar лучше**:
   - Продолжить использовать Ximilar
   - FashionCLIP как fallback

3. **Если примерно одинаково**:
   - Гибридный подход
   - FashionCLIP + Ximilar для консенсуса

## 📝 Лицензия

Этот эксперимент создан для внутреннего использования в проекте trend_ai.
